{
  "name": "markdown-viewer-with-mermaid_chrome-plugin",
  "version": "1.0.0",
  "description": "",
  "main": "background.js",
  "directories": {
    "doc": "doc",
    "lib": "lib"
  },
  "scripts": {
    "test": "echo \"No tests implemented yet\"",
    "lint": "eslint . --ext .js --ignore-path .gitignore",
    "lint:fix": "eslint . --ext .js --ignore-path .gitignore --fix",
    "format": "prettier --write \"**/*.{js,json,md,yml,yaml}\" --ignore-path .gitignore",
    "format:check": "prettier --check \"**/*.{js,json,md,yml,yaml}\" --ignore-path .gitignore",
    "type-check": "tsc --noEmit",
    "docs": "typedoc",
    "docs:watch": "typedoc --watch",
    "validate": "npm run lint && npm run format:check && npm run type-check",
    "prepare": "npm run validate"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/76Hata/markdown-viewer-with-mermaid_chrome-plugin.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "bugs": {
    "url": "https://github.com/76Hata/markdown-viewer-with-mermaid_chrome-plugin/issues"
  },
  "homepage": "https://github.com/76Hata/markdown-viewer-with-mermaid_chrome-plugin#readme",
  "devDependencies": {
    "typedoc": "^0.28.9",
    "eslint": "^8.57.0",
    "prettier": "^3.2.5",
    "typescript": "^5.3.3",
    "@typescript-eslint/eslint-plugin": "^7.0.0",
    "@typescript-eslint/parser": "^7.0.0"
  }
}
